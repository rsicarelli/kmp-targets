org.gradle.caching=true
# Kotlin's built-in abiValidation is configuration-cache compatible, so unlike the abi-bcv sample
# this one keeps the config cache on — the kmp-targets report tasks are CC-safe too.
org.gradle.configuration-cache=true
