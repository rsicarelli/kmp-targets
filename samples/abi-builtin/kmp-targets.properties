# The committed, full selection — the lane that OWNS every target's ABI. The blind spot is
# demonstrated by overriding this with a narrowed lane, e.g. `-Pkmptargets.targets=jvm` (see README).
kmptargets.targets=jvm,linuxX64,js
