org.gradle.caching=true
# The classic kotlinx BCV tasks (apiDump/apiCheck) are not configuration-cache compatible, so the
# config cache is left off in this sample. The kmp-targets report tasks themselves are CC-safe — see
# the abi-builtin sample, which keeps the config cache on with Kotlin's built-in abiValidation.
org.gradle.configuration-cache=false
